classes:
- service.octavia.manager.single
parameters:
  octavia:
    manager:
      enabled: true
      version: ${_param:octavia_version}
      ssh:
        private_key: ${_param:octavia_private_key}
        user: octavia
        group: octavia
